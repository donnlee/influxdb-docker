version: '3'
services:
  grafana:
    image: grafana/grafana:6.7.3
    restart: always
    ports:
      - 3000:3000/tcp
    volumes:
      - /srv/grafana/:/var/lib/grafana
    environment:
      - FOO=bar
    logging:
      options:
        max-size: "10m"
        max-file: "2"
